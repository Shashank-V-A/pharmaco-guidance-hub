# Backend API base URL (no trailing slash). Used by frontend for POST /analyze.
VITE_API_BASE_URL=http://localhost:8000

# Google OAuth 2.0 Client ID (Web application)
# Create at: https://console.cloud.google.com/apis/credentials
# Add authorized JavaScript origins: http://localhost:8080 and https://your-app.vercel.app
# For Vercel: set this in Project → Settings → Environment Variables and redeploy (Vite bakes it at build time).
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
